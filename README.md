# Rational Numbers Calculator
This program functions as a rational number calculator by enabling the user to decide between two arithmetic operations: (A)ddition or (S)ubtraction, then request the input of two rational numbers which will afterward perform the preferred operation and outputs the result in its reduced form.
